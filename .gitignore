# Ignore everything in the project root
*

# But explicitly allow the dashboard directory
!dashboard/
!dashboard/**

# Allow this .gitignore file itself
!.gitignore

# Allow root index redirect for GitHub Pages
!index.html
